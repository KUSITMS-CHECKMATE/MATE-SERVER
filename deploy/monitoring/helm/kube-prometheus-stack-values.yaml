grafana:
  deploymentStrategy:
    # RWO PVC는 단일 Pod만 마운트 가능 → RollingUpdate 대신 Recreate
    type: Recreate
  persistence:
    enabled: true
    type: pvc
    initChownData:
      enabled: false
    accessModes:
      - ReadWriteOnce
    size: 10Gi
    storageClassName: local-path
