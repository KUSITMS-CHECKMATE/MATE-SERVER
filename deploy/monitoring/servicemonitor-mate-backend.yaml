apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mate-backend
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - mate
  selector:
    matchLabels:
      app: mate-backend
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
